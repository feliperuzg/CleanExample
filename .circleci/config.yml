version: 2
jobs:
  build:
    macos:
      xcode: "9.1"

      steps:
        - checkout

        - run:
          name: Build and run tests
          command: fastlane scan
          eviroment:
            SCAN_DEVICE: iPhone 7